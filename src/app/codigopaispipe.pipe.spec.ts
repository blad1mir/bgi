import { CodigopaispipePipe } from './codigopaispipe.pipe';

describe('CodigopaispipePipe', () => {
  it('create an instance', () => {
    const pipe = new CodigopaispipePipe();
    expect(pipe).toBeTruthy();
  });
});
