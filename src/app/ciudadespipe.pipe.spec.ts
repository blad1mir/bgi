import { CiudadespipePipe } from './ciudadespipe.pipe';

describe('CiudadespipePipe', () => {
  it('create an instance', () => {
    const pipe = new CiudadespipePipe();
    expect(pipe).toBeTruthy();
  });
});
