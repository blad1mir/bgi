import { IdCiudadespipePipe } from './id-ciudadespipe.pipe';

describe('IdCiudadespipePipe', () => {
  it('create an instance', () => {
    const pipe = new IdCiudadespipePipe();
    expect(pipe).toBeTruthy();
  });
});
